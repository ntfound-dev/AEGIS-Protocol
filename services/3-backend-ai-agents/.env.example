# Contoh file environment. Salin file ini menjadi .env dan isi nilainya.

# ID Canister dari EventFactory di Internet Computer.
# Nilai ini akan diisi secara otomatis oleh skrip deploy atau Docker Compose.
EVENT_FACTORY_CANISTER_ID=""

# URL untuk replika lokal Internet Computer.
# Untuk pengembangan lokal, biasanya ini tidak perlu diubah.
ICP_URL="http://127.0.0.1:4943"

# Deployment mode for dfx-replica service
# 'auto' = automatically start replica and deploy canisters
# 'manual' = require manual deployment via docker exec
DEPLOY_MODE=auto
